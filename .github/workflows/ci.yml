name: CI

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '9.0.x'

      - name: Restore dependencies
        run: dotnet restore RAGNavigator.sln

      - name: Check vulnerable packages
        run: |
          dotnet list RAGNavigator.sln package --vulnerable --include-transitive > vulnerable-packages.txt
          cat vulnerable-packages.txt
          if grep -q "has the following vulnerable packages" vulnerable-packages.txt; then
            echo "Vulnerable NuGet packages detected."
            exit 1
          fi

      - name: Check deprecated packages
        run: |
          dotnet list RAGNavigator.sln package --deprecated > deprecated-packages.txt
          cat deprecated-packages.txt
          if grep -q "has the following deprecated packages" deprecated-packages.txt; then
            echo "Deprecated NuGet packages detected."
            exit 1
          fi

      - name: Check formatting
        run: dotnet format RAGNavigator.sln --verify-no-changes --verbosity diagnostic

      - name: Build
        run: dotnet build RAGNavigator.sln --no-restore --configuration Release

      - name: Test
        run: dotnet test RAGNavigator.sln --no-build --configuration Release --verbosity normal

  terraform-validate:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: infra

    steps:
      - uses: actions/checkout@v4

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: '~> 1.5'

      - name: Terraform Format Check
        run: terraform fmt -check -recursive

      - name: Terraform Init
        run: terraform init -backend=false

      - name: Terraform Validate
        run: terraform validate
